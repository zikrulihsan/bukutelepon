[build]
builder = "nixpacks"

[deploy]
startCommand = "cd server && npx prisma migrate deploy --schema=../prisma/schema.prisma && node dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
