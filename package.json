{
  "name": "bukutelepon",
  "private": true,
  "scripts": {
    "build": "npx prisma generate && cd server && npm install && npm run build",
    "start": "cd server && npx prisma migrate deploy --schema=../prisma/schema.prisma && node dist/index.js"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  }
}
